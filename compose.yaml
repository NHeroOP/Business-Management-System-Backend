services:
  api:
    image: ghcr.io/NHeroOP/business-management-system-backend:latest
    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "3000:3000"

    container_name: bms-api